# Example environment variables for Neo4j service
# Found in composes/neo4j.yaml

# Authentication: "none" disables auth, "neo4j/yourpassword" sets initial password.
# For production, always set a strong password.
NEO4J_AUTH="none"
# NEO4J_AUTH="neo4j/changemeNOW"

# Memory configuration
NEO4J_DBMS_MEMORY_PAGECACHE_SIZE="1G"
NEO4J_DBMS_MEMORY_HEAP_MAX__SIZE="4G" # Note the double underscore

# Plugins (APOC and Graph Data Science are common)
NEO4JLABS_PLUGINS='["apoc","graph-data-science"]' # JSON array as a string

# If using Neo4j Enterprise Edition with plugins that require it:
# NEO4J_ACCEPT_LICENSE_AGREEMENT="yes"
