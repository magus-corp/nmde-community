# Example environment variables for Typebot stack (builder and viewer)
# Referenced in composes/typebot.yaml and composes/README.md

# Database connection URL (points to shared-postgres-14)
DATABASE_URL="postgresql://typebot:typebot@shared-postgres-14:5432/typebot" # Ensure 'typebot' password matches shared-postgres-14

# Secret for NextAuth.js (generate a strong random string)
NEXTAUTH_SECRET="generatestrongsecrethere" # openssl rand -hex 32

# Public URLs for viewer and builder (replace with your actual domain/IP if exposed)
NEXT_PUBLIC_VIEWER_URL="http://localhost:8081"
NEXT_PUBLIC_BUILDER_URL="http://localhost:8082"

# Encryption key for sensitive data (generate a strong random string of 32 characters)
ENCRYPTION_SECRET="anothersecretkeyof32charslong" # openssl rand -hex 16 (will give 32 hex chars)

# SMTP settings for email functionalities (optional, if you need email features)
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASSWORD=""
# SMTP_FROM_EMAIL=""

# Other Typebot specific settings can be added here as needed.
# Example: Initial admin account
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=strongpassword123
