# Example environment variables for LiteLLM stack
# Found in composes/litellm.yaml

# For the 'litellm' service
DATABASE_URL="postgresql://llmproxy:dbpassword9090@db:5432/litellm"
STORE_MODEL_IN_DB="True"
# Note: The litellm service also uses `env_file: - .env`. 
# Additional variables from that .env file might be needed.
# Example: OPENAI_API_KEY="your_openai_api_key"

# For the 'db' service (PostgreSQL)
POSTGRES_DB="litellm"
POSTGRES_USER="llmproxy"
POSTGRES_PASSWORD="dbpassword9090"
