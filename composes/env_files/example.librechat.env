# Example environment variables for LibreChat stack
# Based on composes/librechat.yaml and common LibreChat .env variables.
# This file is intended as an example; LibreChat typically uses a .env file
# in the same directory as its docker-compose.yaml (or specified by `source: ./.env`).

# For 'api' service:
PORT="3080" # Port the LibreChat API will listen on
HOST="0.0.0.0"

# MongoDB connection string (points to shared-mongodb)
MONGO_URI="mongodb://magus:senhamagus123!@shared-mongodb:27017/LibreChat?authSource=admin" # Ensure credentials match shared-mongodb

# MeiliSearch connection
MEILI_HOST="http://meilisearch:7700"

# RAG API connection
RAG_PORT="8000" # Ensure this matches the RAG_PORT for the rag_api service
RAG_API_URL="http://rag_api:8000" # URL for the RAG API service

# User and Group ID for running the service (optional, for permission handling)
# UID="1000"
# GID="1000"

# For 'meilisearch' service:
# MEILI_MASTER_KEY="your_strong_meilisearch_master_key" # Set a master key for production
MEILI_NO_ANALYTICS="true"

# For 'rag_api' service:
# Database connection for RAG API (points to shared-postgres-pgvector)
DB_HOST_RAG="shared-postgres-pgvector" # Updated to new shared service name
DB_PORT_RAG="5432" # Default PostgreSQL port
DB_USER_RAG="myuser" # Ensure this matches shared-postgres-pgvector config
DB_PASSWORD_RAG="mypassword" # Ensure this matches shared-postgres-pgvector config
DB_NAME_RAG="mydatabase" # Ensure this matches shared-postgres-pgvector config

# Add other necessary LibreChat environment variables here, for example:
# OPENAI_API_KEY="sk-yourkey"
# GOOGLE_API_KEY="yourkey"
# ANTHROPIC_API_KEY="yourkey"
# AZURE_OPENAI_API_KEY="yourkey"
# AZURE_OPENAI_API_INSTANCE_NAME="yourinstancename"
# AZURE_OPENAI_API_DEPLOYMENT_NAME="yourdeploymentname"
# AZURE_OPENAI_API_VERSION="api-version"
# DOMAIN_CLIENT="http://localhost:3080"
# DOMAIN_SERVER="http://localhost:3080"

# For custom endpoints, search, etc.
# Refer to the official LibreChat documentation for a full list of variables.
